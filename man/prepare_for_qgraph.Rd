% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity_utils.R
\name{prepare_for_qgraph}
\alias{prepare_for_qgraph}
\title{Prepare Matrix for qgraph Visualization}
\usage{
prepare_for_qgraph(
  distance_matrix,
  conversion_method = "normalized_inverse",
  normalization = "minmax",
  target_range = c(0, 1),
  threshold_method = "none",
  threshold_value = 0.7
)
}
\arguments{
\item{distance_matrix}{Distance matrix}

\item{conversion_method}{Method for distance-to-similarity conversion}

\item{normalization}{Whether to normalize ("none", "minmax", "max")}

\item{target_range}{Target range for final values}

\item{threshold_method}{How to handle thresholding ("none", "percentile", "absolute")}

\item{threshold_value}{Threshold value (percentile 0-1 or absolute value)}
}
\value{
Matrix ready for qgraph visualization with metadata
}
\description{
One-stop function to prepare distance matrices for qgraph visualization.
Combines distance-to-similarity conversion and normalization.
}
\examples{
# Large distance matrix (17,000 to 50,000 range)
set.seed(123)
large_distances <- matrix(runif(25, 17000, 50000), nrow = 5)
large_distances <- (large_distances + t(large_distances)) / 2  # Make symmetric
diag(large_distances) <- 0

# Prepare for qgraph
viz_matrix <- prepare_for_qgraph(large_distances)

# Use with qgraph
if (requireNamespace("qgraph", quietly = TRUE)) {
  qgraph::qgraph(viz_matrix, layout = "spring", threshold = 0.7)
}
}
