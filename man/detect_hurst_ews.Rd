% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurst_analysis.R
\name{detect_hurst_ews}
\alias{detect_hurst_ews}
\title{Advanced Early Warning Signal Detection with Graded Risk Levels}
\usage{
detect_hurst_ews(
  result_df,
  method = NULL,
  extreme_percentiles = c(0.05, 0.95),
  trend_percentile = 0.9,
  volatility_percentile = 0.9,
  trend_window = 30,
  volatility_window = 30,
  flicker_window = 20,
  use_multivariate = TRUE,
  use_variance_ratio = TRUE,
  use_spectral = TRUE,
  append_to_data = TRUE
)
}
\arguments{
\item{result_df}{Data frame with Hurst analysis results}

\item{method}{Method used for Hurst analysis (auto-detected if NULL)}

\item{extreme_percentiles}{Percentiles for extreme value detection}

\item{trend_percentile}{Percentile threshold for trend detection}

\item{volatility_percentile}{Percentile threshold for volatility detection}

\item{trend_window}{Window size for trend analysis}

\item{volatility_window}{Window size for volatility analysis}

\item{flicker_window}{Window size for flickering detection}

\item{use_multivariate}{Whether to use multivariate indicators}

\item{use_variance_ratio}{Whether to use variance ratio test}

\item{use_spectral}{Whether to use spectral analysis}

\item{append_to_data}{Whether to append results to input data}
}
\value{
Data frame with early warning signal indicators
}
\description{
Detects early warning signals using multiple sophisticated indicators
and assigns graded warning levels (0-4) to each time point:
- 0: None (no warning)
- 1: Low (minor indicators present)
- 2: Moderate (multiple indicators or moderate severity)
- 3: High (strong indicators present)
- 4: Critical (multiple strong indicators)
}
\examples{
# Generate sample data with regime changes
set.seed(123)
data <- data.frame(
  time = 1:500,
  value = cumsum(c(rnorm(200), rnorm(100, sd = 2), rnorm(200)))
)

# Calculate Hurst states
hurst_result <- calculate_hurst(data, ts_col = "value", method = "dfa")

# Detect early warning signals
ews_result <- detect_hurst_ews(hurst_result)
table(ews_result$ews_category)

}
