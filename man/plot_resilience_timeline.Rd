% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resilience_plotting.R
\name{plot_resilience_timeline}
\alias{plot_resilience_timeline}
\title{Plot Resilience Capacity Timeline}
\usage{
plot_resilience_timeline(
  data,
  capacity_type = c("all", "absorptive", "restorative", "adaptive", "composite"),
  ts_col = NULL,
  time_col = NULL,
  state_col = NULL,
  facet_by = NULL,
  smooth_line = TRUE,
  color_palette = "viridis",
  title = NULL
)
}
\arguments{
\item{data}{Data frame with resilience metrics (from calculate_resilience_metrics())}

\item{capacity_type}{Which capacity to plot: "all", "absorptive", "restorative", "adaptive", "composite"}

\item{ts_col}{Time series column name (auto-detected if NULL)}

\item{time_col}{Time column name (auto-detected if NULL)}

\item{state_col}{Resilience state column for shading (optional)}

\item{facet_by}{Column to facet by (optional)}

\item{smooth_line}{Whether to add smoothed trend line (default: TRUE)}

\item{color_palette}{Color palette to use (default: "viridis")}

\item{title}{Plot title (auto-generated if NULL)}
}
\value{
ggplot object compatible with existing TSnetworks plots
}
\description{
Creates resilience capacity plots using TSnetworks plotting infrastructure.
This function extends the existing plot_timeseries_enhanced() functionality
to visualize resilience capacity metrics over time.
}
\examples{
# Basic resilience capacity timeline
data(saqrsteps)
resilience_data <- calculate_resilience_metrics(saqrsteps, ts_cols = "Steps")
state_data <- classify_resilience_states(resilience_data)

# Plot all capacities
plot_resilience_timeline(state_data, capacity_type = "all")

# Plot specific capacity with state shading
plot_resilience_timeline(state_data, capacity_type = "absorptive", 
                        state_col = "resilience_state")

}
