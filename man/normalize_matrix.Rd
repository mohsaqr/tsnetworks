% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity_utils.R
\name{normalize_matrix}
\alias{normalize_matrix}
\title{Normalize Matrix for Visualization}
\usage{
normalize_matrix(
  matrix,
  method = "minmax",
  target_range = c(0, 1),
  exclude_diagonal = TRUE,
  preserve_zeros = TRUE
)
}
\arguments{
\item{matrix}{Input matrix (distance or similarity)}

\item{method}{Normalization method:
- "minmax": (x - min) / (max - min) → [0,1]
- "max": x / max(x) → [0,1] 
- "zscore": (x - mean) / sd → standardized
- "robust": (x - median) / mad → robust standardization
- "quantile": scale to quantile range}

\item{target_range}{Target range for output (e.g., c(0,1))}

\item{exclude_diagonal}{Exclude diagonal from normalization calculations}

\item{preserve_zeros}{Whether to keep zero values as zero}
}
\value{
Normalized matrix
}
\description{
Normalize matrix values to appropriate range for network visualization
}
\examples{
# Large distance matrix
distances <- matrix(runif(9, 1000, 50000), nrow = 3)

# Normalize to 0-1 range
norm_minmax <- normalize_matrix(distances, "minmax")
norm_max <- normalize_matrix(distances, "max")
}
